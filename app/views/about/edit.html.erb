<% if @about.errors.any? %>
  <div class="alert alert-danger" role="alert" id="error_explanation">
    <h4 class="alert-heading">
    	<%= pluralize(@about.errors.count, 'error') %> prohibited this article from being save: 
    </h4>
    <hr/>
    <ul>
        <% @about.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
    </ul>
  </div>
<% end %>

<section class="container pt-5">
  <div class="row pt-5">
      <div class="col-12">

<h2>Edytuj opis</h2>
<%= form_for @about, html: {multipart: true} do |f| %>
	<p>
		<%= f.label :Prezes %><br>
		<%= f.text_field :president, class: 'form-control' %>
	<p>
		<%= f.label :Viceprezes %><br>
		<%= f.text_field :vice_president, class: 'form-control' %>
	</p>
	<p>
		<%= f.label :Skarbnik%><br>
		<%= f.text_field :treasure, class: 'form-control' %>
	</p>
	<p>
		<%= f.submit "Zapisz", class: 'btn btn-md btn-success' %>
	</p>
	<% end %>
		<p>
			<%= link_to 'Powrót', about_index_path, class: 'btn btn-md btn-info' %>
		</p>
	</div>
	<div class="col"></div>
</div>

</section>
