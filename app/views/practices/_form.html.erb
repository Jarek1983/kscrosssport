<%= form_with(model: practice, local: true, html: {multipart: true}) do |form| %>
  <% if practice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(practice.errors.count, "error") %> prohibited this practice from being saved:</h2>

      <ul>
      <% practice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle %>
  </div>

  <div class="field">
    <%= form.label :boxtitle %>
    <%= form.text_field :boxtitle %>
  </div>

    <div class="field">
    <%= form.label :picone %>
    <%= form.file_field :picone %>
  </div>

  <div class="field">
    <%= form.label :boxinfo %>
    <%= form.text_area :boxinfo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
